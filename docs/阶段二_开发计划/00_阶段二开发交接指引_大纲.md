# 阶段二开发交接指引（大纲）

## 文档信息

- **文档目的**：把已通过的阶段一设计交接给下一位开发者或智能体，说明开始编码前必须读取的依据、阶段二实施主线、质量要求和完成标准。
- **当前状态**：大纲，待展开。
- **适用范围**：阶段二完整代码实现，以及进入最终联调、测试和交付前的准备工作。
- **需求基线**：`E:\Work\Student_club\all_\需求问答-修改版(严格按要求修改).md`。
- **设计依据**：`E:\Work\Student_club\all_\docs\阶段一_系统设计\01_角色权限矩阵.md` 至 `07_阶段一一致性审查.md`。
- **交接原则**：阶段一已确认的权限、状态、实体、字段、API 和页面规则不得由开发者自行改变；发现冲突时先停止编码并提交用户确认。

## 1. 交接目标

本节后续展开：

- 下一位开发者接手时应掌握的项目背景。
- 阶段一已经完成并通过的成果。
- 阶段二需要交付的完整可运行系统。
- 阶段二之后只保留最终联调、测试和交付，不再增加新的大阶段。

## 2. 开始编码前的必读顺序

本节后续展开以下阅读顺序和每份文档的用途：

1. 唯一需求基线。
2. `01_角色权限矩阵.md`。
3. `02_业务状态与状态流转.md`。
4. `03_数据库概念设计.md`。
5. `04_数据库逻辑设计.md`。
6. `05_API接口设计.md`。
7. `06_页面菜单与路由设计.md`。
8. `07_阶段一一致性审查.md`。
9. 本交接指引。

## 3. 已确认且不得重新决定的规则

本节后续汇总：

- Django、Vue 3、Element Plus、MySQL 和在线 DeepSeek API。
- Django Session、`HttpOnly` Cookie。
- `code`、`message`、`data` 统一响应包装。
- `page`、`page_size` 页码分页。
- 13 个业务实体及禁止新增的需求外实体。
- 社团名称唯一。
- 帖子标题、帖子正文和回复的最大长度。
- DeepSeek 模型、参数和密钥只位于后端环境配置。
- 负责人概览只统计正常帖子。
- 最新帖子使用自增 ID 表示发布先后。
- 意见反馈处理说明可选，内容举报处理说明必填。

## 4. 阶段二范围

### 4.1 必须完成

本节后续按功能列出：

- 后端工程、前端工程和 MySQL 数据库。
- 认证、用户资料和管理员用户管理。
- 社团、成员关系和负责人管理。
- 招新和入社申请。
- 公告、帖子、回复和帖子点赞。
- 三类站内通知。
- 社团评价、意见反馈和内容举报。
- 帖子 AI 和 AI 文档生成。
- 管理员、负责人和学生数据概览。
- 全部规定页面、菜单、路由和权限守卫。

### 4.2 明确不做

本节后续引用阶段一禁止范围，重点包括：

- 活动管理、实时聊天、私聊和在线状态。
- 独立前台首页、深色模式和专门的移动端适配。
- 帖子分类、帖子修改、多层回复和回复点赞。
- 通知已读、业务跳转和通用对象关联。
- AI 历史、复杂 RAG、向量知识库和 AI 自动发布。
- 需求外统计图表、审计日志和状态历史。

## 5. 项目结构与工程约定

本节后续明确：

- `backend/` 与 `frontend/` 的目录结构。
- Django 应用拆分方式。
- Vue 页面、组件、路由、状态和 API 客户端的组织方式。
- 测试目录和测试数据组织方式。
- 环境配置、示例配置和敏感信息管理方式。
- 依赖版本的确认、锁定和升级规则。

## 6. 阶段二实施主线

本节只按一条开发主线组织工作，不再建立额外的大阶段：

1. 初始化后端、前端、数据库和测试环境。
2. 在首次迁移前建立自定义用户模型。
3. 完成统一响应、分页、异常处理和 Session 基础。
4. 完成注册、登录、本人资料和管理员用户管理闭环。
5. 按业务依赖依次实现其余功能模块。
6. 完成前后端联调、权限校验和异常路径。
7. 完成构建、测试、示例数据和运行说明。

## 7. 第一批编码任务

本节后续展开第一批可以直接执行的任务：

- 确认并锁定开发工具和依赖版本。
- 初始化 Django 与 Vue 工程。
- 建立环境变量示例文件。
- 配置 MySQL、Session、Cookie 和前后端开发环境通信。
- 创建自定义用户模型，设置 `AUTH_USER_MODEL`。
- 建立统一响应、统一异常和分页基础。
- 建立后端测试与前端构建检查。

第一批结束时，不要求完成全部业务，但项目必须能够启动、连接数据库并运行基础测试。

## 8. 功能实现与验收模板

本节后续为每个功能模块统一规定：

- 实现目标。
- 依赖的阶段一规则。
- 涉及的数据表、API 和页面。
- 正常流程验收条件。
- 权限、状态和并发异常验收条件。
- 后端测试、前端测试或浏览器检查方式。
- 完成后需要更新的交接记录。

## 9. 测试与质量门槛

本节后续明确：

- 模型约束和迁移检查。
- API 正常流程、权限拒绝和业务错误测试。
- Session 和账号停用测试。
- 最后负责人、招新容量和唯一约束的并发复查。
- 前端路由守卫、表单校验、空状态和错误状态。
- DeepSeek 成功、截断和调用失败场景。
- 前端构建、后端测试和关键流程端到端验收。

## 10. 环境变量与安全边界

本节后续列出变量名称和用途，但不记录真实密钥：

- Django 密钥和运行环境。
- MySQL 连接信息。
- Cookie 和跨域配置。
- DeepSeek API 地址、密钥、模型和请求参数。
- 前端 API 基础地址。

真实密码和 API 密钥不得写入代码、Markdown 文档或版本库。

## 11. 进度记录与再次交接

本节后续规定每次结束工作前记录：

- 已完成模块。
- 未完成模块。
- 已执行的迁移。
- 已通过和未通过的测试。
- 当前已知问题。
- 临时技术决定及其依据。
- 下一位开发者应执行的第一条命令或第一项任务。

## 12. 阶段二完成定义

阶段二只有同时满足以下条件才能报告完成：

- [ ] 阶段一规定的全部后端功能已经实现。
- [ ] 阶段一规定的全部前端页面和操作入口已经实现。
- [ ] 数据库迁移能够从空数据库完整执行。
- [ ] 69 个 API 与页面调用保持一致。
- [ ] 权限、状态、副作用和并发保护符合阶段一设计。
- [ ] DeepSeek 两类功能符合数据和持久化边界。
- [ ] 后端测试通过，前端构建通过，关键流程完成联调。
- [ ] 示例环境配置和本地启动说明完整。
- [ ] 没有擅自加入阶段一明确排除的功能。
- [ ] 已留下足够的进度和问题记录供最终测试与交付使用。

## 13. 本大纲的下一步

后续只需在本文件中补充：

1. 实际项目目录结构。
2. 具体依赖与版本。
3. Django 应用拆分。
4. 第一批编码任务的文件级验收标准。
5. 测试命令和运行命令。
6. 开发过程中的进度记录格式。

大纲展开完成并经用户确认后，即可开始创建项目和编写代码。
